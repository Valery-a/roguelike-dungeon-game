@page "/"

<div style="display: flex; justify-content: center; align-items: center; flex-direction: column;">
    <h1>WELCOME TO RDCG</h1>
    <div class="game-intro">
        <div class="intro-box">
            <p>Prepare yourself for a journey into a mesmerizing world where magic and adventure await at every turn. RDCG offers a unique blend of thrilling quests, challenging puzzles, and intense combat. Will you rise to the occasion and become the hero this realm needs?</p>
        </div>
    </div>
    <div class="features">
        <div class="features-column">
            <ul>
                <li>Immersive storyline with lore</li>
                <li>Expansive world to explore</li>
                <li>Dynamic combat system</li>
            </ul>
        </div>
        <div class="features-column">
            <ul>
                <li>Challenging dungeons</li>
                <li>Gear upgrades </li>
                <li>Many ways to level up</li>
            </ul>
        </div>
    </div>
    <div class="call-to-action">
        <p>Keep in mind that there are no character saves in the web version!</p>
        <p>To start the game you need to click inside the console box and press [ENTER].</p>
        <p>You can skip the time for text output time by pressking [ENTER]</p>
        <a href="BlazorDungeon" class="btn btn-primary">BEGIN</a>
    </div>
</div>

<style>
    /* Style the page layout as per your requirements */
    body {
        background-color: #131213;
        color: white;
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
    }

    /* Center the content vertically and horizontally */
    .container {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        height: 100vh;
        padding: 2rem;
        box-sizing: border-box;
        text-align: center;
    }

    /* Heading styles */
    h1 {
        font-size: 3rem;
        margin-bottom: 2rem;
    }

    /* Introduction box styles */
    .intro-box {
        padding: 2rem;
        margin: 2rem;
        background-color: rgba(255, 255, 255, 0.1);
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
    }

    /* Features section styles */
    .features {
        display: flex;
        justify-content: space-between;
        margin-bottom: 2rem;
    }

    .features-column {
        flex: 0 0 48%;
    }

    /* List styles */
    ul {
        list-style-type: none;
        padding: 0;
        margin: 0;
    }

    li:before {
        content: "\2022";
        color: #ffd700; /* Use a gold color for bullet points */
        display: inline-block;
        width: 1em;
        margin-left: -1em;
    }

    /* Add spacing between list items */
    li {
        margin-bottom: 1rem; /* Increase the margin-bottom value for more spacing */
    }

    /* Call-to-action section styles */
    .call-to-action {
        margin-top: 2rem;
        text-align: center;
    }

    .btn {
        display: inline-block;
        padding: 1rem 2rem;
        font-size: 1.2rem;
        font-weight: bold;
        text-decoration: none;
        text-transform: uppercase;
        background-color: #ffd700;
        color: #131213;
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

    /* Center the button */
    .btn-container {
        display: flex;
        justify-content: center;
        margin-top: 1rem;
    }

    .btn:hover {
        background-color: #e6be00; /* Darken the button color on hover */
    }
</style>
