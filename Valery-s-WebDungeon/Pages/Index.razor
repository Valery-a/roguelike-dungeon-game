@page "/"

<div style="display: flex; justify-content: center; align-items: center; flex-direction: column;">
    <h1 style="font-family: 'Impact'; text-align: center; font-size: 5rem; color: blue; text-shadow: 0 0 10px blue; animation: pulse 1s infinite;">WELCOME TO RDCG</h1>

    <div class="game-intro">
        <div class="intro-box">
            <p style="font-family: 'Comic Sans MS', cursive; font-size: 1.5rem; color: #919100;">Prepare yourself for a journey into a mesmerizing world where magic and adventure await at every turn. RDCG offers a unique blend of thrilling quests, challenging puzzles, and intense combat. Will you rise to the occasion and become the hero this realm needs?</p>
        </div>
    </div>
    <a href="BlazorDungeon" class="btn btn-primary pulse" style="color: gray;">BEGIN</a>
    <div class="call-to-action">
        <p style="font-size: 1.2rem;">Keep in mind that there are no character saves in the web version!</p>
        <p style="font-size: 1.2rem;">To start the game you need to click inside the console box and press [ENTER].</p>
        <p style="font-size: 1.2rem;">You can skip the time for text output time by pressing [ENTER]</p>
    </div>
</div>
<style>
    /* Style the page layout as per your requirements */
    body {
        background-color: #131213;
        color: white;
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
    }

    /* Center the content vertically and horizontally */
    .container {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        height: 100vh;
        padding: 2rem;
        box-sizing: border-box;
        text-align: center;
    }

    /* Heading styles */
    h1 {
        font-size: 3rem;
        margin-bottom: 2rem;
    }

    /* Introduction box styles */
    .intro-box {
        padding: 2rem;
        margin: 2rem;
        background-color: rgba(255, 255, 255, 0.1);
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
    }

    /* Features section styles */
    .features {
        display: flex;
        justify-content: space-between;
        margin-bottom: 2rem;
    }

    .features-column {
        flex: 0 0 48%;
    }

    p {
        text-align: center;
    }


    .call-to-action {
        text-align: center;
        padding: 2rem;
        margin: 2rem;
        background-color: rgba(255, 255, 255, 0.1);
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
    }

    .btn {
        display: inline-block;
        padding: 1rem 2rem;
        font-size: 1.2rem;
        font-weight: bold;
        text-decoration: none;
        text-transform: uppercase;
        background-color: #ffd700;
        color: #131213;
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

    /* Center the button */
    .btn-container {
        display: flex;
        justify-content: center;
        margin-top: 1rem;
    }

    .btn:hover {
        background-color: #e6be00; /* Darken the button color on hover */
    }

    .pulse {
        animation: pulse 1s infinite;
    }
</style>